#Instalações

**Crie um ambiente virtual com o python -m venv .venv**
--Após criar o ambiente faça

*pip install "fastapi[standard]"
*pip install PyJWT databases sqlalchemy aiosqlite pwdlib[argon2] pydantic-settings